version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm
        - corepack enable
        - npx --yes nypm i
    build:
      commands:
        - pnpm run build
        - pnpm run start
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
